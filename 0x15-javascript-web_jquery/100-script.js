document.addEventListener('DOMContentLoaded', function () {
  const header = document.querySelector('header'); // Select the header tag
  if (header) {
    header.style.color = '#FF0000'; // Set text color to red
  }
});

